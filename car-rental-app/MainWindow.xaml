<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="car_rental_app.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:car_rental_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <!-- Left column for buttons -->
            <ColumnDefinition Width="*"/>
            <!-- Right column for frame -->
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="RentedCarsStackPanel" Grid.Column="0">
            <TextBlock x:Name="navigation" Text="Navigation" Margin="16" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Testing" />
            <Button Click="AddCar">Add car</Button>
        </StackPanel>
        <Frame x:Name="ContentFrame" Grid.Column="1"/>
    </Grid>
</Window>
