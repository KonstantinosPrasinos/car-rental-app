<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="car_rental_app.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:car_rental_app"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Name="MainWindowGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar" Grid.Row="0" Grid.ColumnSpan="2" Grid.Column="0" Background="Transparent">
            <TextBlock Text="Car rental app" Style="{ThemeResource CaptionTextBlockStyle}" Margin="12 12" Grid.Column="0" />
        </Grid>
        
        <Frame x:Name="ContentFrame" Grid.Row="1"/>
    </Grid>
</Window>
